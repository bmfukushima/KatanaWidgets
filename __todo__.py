"""
TODO: Enhancement
    *   PopupBar Widget fix image overlays on show
    *   VariableManager | Can load multiple patterns
            Remake this?
TODO: BUGS
    *   Node Graph Pins
            Opening same file that is already open clears pins.
    *   Backdrops
            Layering is only valid for backdrops in stack,
            if a backdrop in the stack is overlapping another backdrop,
            it will not update to the correct depth.
"""
